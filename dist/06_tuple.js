"use strict";
// Tuple
// Number of elements are fixed
// types of elements are known and same
let skill = ["programming", 34];
console.log(skill);
console.log(skill[0]);
// (rgba)                          
let color = [255, 255, 0, 0.4];
console.log("Current color is ", color);
// optional Tuple:                  // optional
let colors = [255, 255, 0];
